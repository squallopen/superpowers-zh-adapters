# v0.3.2 上游同步小更新

这版不是大改，主要是把上游 `obra/superpowers` 跟到 `v5.0.7`。

直接说重点：

- 我们仓库里 vendored 的上游内容现在更新到 `v5.0.7`
- 这轮上游最相关的是 `OpenCode` 那边的一些修正，我们也一起带上了
- 现有支持的 `Cline`、`Codex`、`Claude Code`、`Droid`、`OpenCode`、`CodeBuddy` 这几条线，不需要你重新理解一套新玩法

如果你已经在用这套适配，这版可以理解成一次“顺手更新到更稳一点”的小升级。
